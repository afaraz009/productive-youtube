/**
 * Passive Homepage Blocker
 * Relying on CSS classes for hiding to avoid layout thrashing and DOM conflicts.
 */
export function removeHomepageVideos(): void {
  // Logic moved to CSS: html.hide-homepage handles this professionally.
}

export function throttledRemoveHomepageVideos(): void {
  // Logic moved to CSS
}
