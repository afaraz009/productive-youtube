/**
 * Passive Shorts Blocker
 * Relying on CSS classes for hiding to avoid layout thrashing and DOM conflicts.
 */
export function removeShorts(): void {
  // Logic moved to CSS: html.hide-shorts handles this professionally.
}

export function throttledRemoveShorts(): void {
  // Logic moved to CSS
}
