/**
 * Passive Shorts Button Blocker
 * Relying on CSS classes for hiding to avoid layout thrashing and DOM conflicts.
 */
export function removeShortsButton(): void {
  // Logic moved to CSS: html.hide-shorts-button handles this professionally.
}

export function throttledRemoveShortsButton(): void {
  // Logic moved to CSS
}
