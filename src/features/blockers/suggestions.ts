/**
 * Passive Video Suggestions Blocker
 * Relying on CSS classes for hiding to avoid layout thrashing and DOM conflicts.
 */
export function removeVideoSuggestions(): void {
  // Logic moved to CSS: html.hide-suggestions handles this professionally.
}

export function throttledRemoveVideoSuggestions(): void {
  // Logic moved to CSS
}
